import React from "react";
import './NavBar.css';

function NavBar() {
    return(
        <nav>
            <a href="#">Home</a>
            <a href="#">About Me</a>
            <a href="#">Articles</a>
            <a href="#">Ajaira</a>
        </nav>
    );
}

export default NavBar;